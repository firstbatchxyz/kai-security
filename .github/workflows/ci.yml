name: CI

on:
  workflow_dispatch:
  pull_request:
    branches: ["dev", "master"]
    paths: ["src/**", "tests/**", "pyproject.toml", ".github/workflows/ci.yml"]
  push:
    branches: ["dev", "master"]
    paths: ["src/**", "tests/**", "pyproject.toml", ".github/workflows/ci.yml"]

jobs:
  quality:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      PYTEST_DISABLE_PLUGIN_AUTOLOAD: "1"

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Setup uv
        uses: astral-sh/setup-uv@v4
        with:
          enable-cache: true
          cache-dependency-glob: |
            pyproject.toml
            uv.lock

      - name: Install dependencies
        run: uv sync --group dev --extra railway --extra cybergym

      - name: Lint
        run: uv run ruff check src tests evaluation

      - name: Typecheck
        run: uv run ty check src evaluation

      - name: Test
        run: uv run pytest -q -p pytest_asyncio.plugin
